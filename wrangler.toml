name = "mithur-2-0"
pages_build_output_dir = "."
compatibility_date = "2024-09-01"

# Esta sección conecta tu base de datos (Cloudflare KV) con el código.
# Reemplaza "id" por el ID real que te da Cloudflare al crear el namespace
# (ver README.md, paso 4). El "binding" debe llamarse COMIC_KV porque
# así se usa en las funciones dentro de /functions/api.
[[kv_namespaces]]
binding = "COMIC_KV"
id = "47b5579e04c846aea1e1fe739a530d52"
